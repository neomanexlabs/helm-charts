{{- if or (eq .Values.architecture "standalone") (eq .Values.architecture "replication") }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "redis.master.statefulsetName" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "redis.labels" . | nindent 4 }}
    app.kubernetes.io/component: master
    {{- if .Values.master.podLabels }}
    {{- toYaml .Values.master.podLabels | nindent 4 }}
    {{- end }}
  {{- if .Values.master.podAnnotations }}
  annotations: {{- toYaml .Values.master.podAnnotations | nindent 4 }}
  {{- end }}
spec:
  serviceName: {{ include "redis.headless.serviceName" . }}
  replicas: {{ .Values.master.count }}
  selector:
    matchLabels: {{- include "redis.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: master
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels: {{- include "redis.labels" . | nindent 8 }}
        app.kubernetes.io/component: master
        {{- if .Values.master.podLabels }}
        {{- toYaml .Values.master.podLabels | nindent 8 }}
        {{- end }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/scripts: {{ include (print $.Template.BasePath "/scripts-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- if .Values.master.podAnnotations }}
        {{- toYaml .Values.master.podAnnotations | nindent 8 }}
        {{- end }}
    spec:
      {{- include "redis.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "redis.serviceAccountName" . }}
      {{- if .Values.master.priorityClassName }}
      priorityClassName: {{ .Values.master.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.master.affinity }}
      affinity: {{- toYaml .Values.master.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.master.nodeSelector }}
      nodeSelector: {{- toYaml .Values.master.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.master.tolerations }}
      tolerations: {{- toYaml .Values.master.tolerations | nindent 8 }}
      {{- end }}
      {{- with .Values.master.topologySpreadConstraints }}
      topologySpreadConstraints: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.master.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.master.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      {{- if or .Values.master.initContainers .Values.sysctl.enabled .Values.volumePermissions.enabled }}
      initContainers:
        {{- if .Values.volumePermissions.enabled }}
        - name: volume-permissions
          image: {{ .Values.volumePermissions.image.registry }}/{{ .Values.volumePermissions.image.repository }}:{{ .Values.volumePermissions.image.tag }}
          imagePullPolicy: {{ .Values.volumePermissions.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              chown -R 1001:1001 /data
              chmod -R 755 /data
          securityContext:
            runAsUser: 0
          resources: {{- toYaml .Values.volumePermissions.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
        {{- end }}
        {{- if .Values.sysctl.enabled }}
        - name: init-sysctl
          image: {{ .Values.sysctl.image.registry }}/{{ .Values.sysctl.image.repository }}:{{ .Values.sysctl.image.tag }}
          imagePullPolicy: {{ .Values.sysctl.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              {{- range $key, $value := .Values.sysctl.sysctls }}
              sysctl -w {{ $key }}={{ $value }}
              {{- end }}
          securityContext:
            privileged: true
            runAsUser: 0
          resources: {{- toYaml .Values.sysctl.resources | nindent 12 }}
        {{- end }}
        {{- if .Values.master.initContainers }}
        {{- toYaml .Values.master.initContainers | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        - name: redis
          image: {{ include "redis.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.master.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.master.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.master.command }}
          command: {{- toYaml .Values.master.command | nindent 12 }}
          {{- else }}
          command:
            - /bin/sh
            - /opt/redis/scripts/start-master.sh
          {{- end }}
          env:
            - name: REDIS_REPLICATION_MODE
              value: master
            {{- if .Values.master.extraEnvVars }}
            {{- toYaml .Values.master.extraEnvVars | nindent 12 }}
            {{- end }}
          {{- if or .Values.master.extraEnvVarsCM .Values.master.extraEnvVarsSecret }}
          envFrom:
            {{- if .Values.master.extraEnvVarsCM }}
            - configMapRef:
                name: {{ .Values.master.extraEnvVarsCM }}
            {{- end }}
            {{- if .Values.master.extraEnvVarsSecret }}
            - secretRef:
                name: {{ .Values.master.extraEnvVarsSecret }}
            {{- end }}
          {{- end }}
          ports:
            - name: redis
              containerPort: {{ include "redis.containerPort" . }}
              protocol: TCP
          {{- if .Values.master.livenessProbe.enabled }}
          livenessProbe:
            {{- if .Values.master.customLivenessProbe }}
            {{- toYaml .Values.master.customLivenessProbe | nindent 12 }}
            {{- else }}
            exec:
              command:
                - sh
                - -c
                - |
                  {{- if (include "redis.auth.enabled" .) }}
                  redis-cli -a "$( cat /opt/redis/password/redis-password )" ping | grep -q PONG
                  {{- else }}
                  redis-cli ping | grep -q PONG
                  {{- end }}
            initialDelaySeconds: {{ .Values.master.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.master.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.master.livenessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.master.livenessProbe.successThreshold }}
            failureThreshold: {{ .Values.master.livenessProbe.failureThreshold }}
            {{- end }}
          {{- end }}
          {{- if .Values.master.readinessProbe.enabled }}
          readinessProbe:
            {{- if .Values.master.customReadinessProbe }}
            {{- toYaml .Values.master.customReadinessProbe | nindent 12 }}
            {{- else }}
            exec:
              command:
                - sh
                - /opt/redis/scripts/ping-readiness.sh
            initialDelaySeconds: {{ .Values.master.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.master.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.master.readinessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.master.readinessProbe.successThreshold }}
            failureThreshold: {{ .Values.master.readinessProbe.failureThreshold }}
            {{- end }}
          {{- end }}
          {{- if .Values.master.startupProbe.enabled }}
          startupProbe:
            {{- if .Values.master.customStartupProbe }}
            {{- toYaml .Values.master.customStartupProbe | nindent 12 }}
            {{- else }}
            exec:
              command:
                - sh
                - -c
                - |
                  {{- if (include "redis.auth.enabled" .) }}
                  redis-cli -a "$( cat /opt/redis/password/redis-password )" ping | grep -q PONG
                  {{- else }}
                  redis-cli ping | grep -q PONG
                  {{- end }}
            initialDelaySeconds: {{ .Values.master.startupProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.master.startupProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.master.startupProbe.timeoutSeconds }}
            successThreshold: {{ .Values.master.startupProbe.successThreshold }}
            failureThreshold: {{ .Values.master.startupProbe.failureThreshold }}
            {{- end }}
          {{- end }}
          resources: {{- toYaml .Values.master.resources | nindent 12 }}
          {{- if .Values.master.lifecycleHooks }}
          lifecycle: {{- toYaml .Values.master.lifecycleHooks | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /opt/redis/redis.conf
              subPath: redis.conf
            - name: scripts
              mountPath: /opt/redis/scripts
              readOnly: true
            - name: tmp
              mountPath: /tmp
            {{- if (include "redis.auth.enabled" .) }}
            - name: redis-password
              mountPath: /opt/redis/password
              readOnly: true
            {{- end }}
            {{- if .Values.master.extraVolumeMounts }}
            {{- toYaml .Values.master.extraVolumeMounts | nindent 12 }}
            {{- end }}
        {{- if .Values.metrics.enabled }}
        - name: metrics
          image: {{ include "redis.metrics.image" . }}
          imagePullPolicy: {{ .Values.metrics.image.pullPolicy }}
          securityContext:
            runAsUser: 1001
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          command:
            - /bin/sh
            - -c
            - |
              {{- if (include "redis.auth.enabled" .) }}
              export REDIS_PASSWORD="$( cat /opt/redis/password/redis-password )"
              /redis_exporter \
                --redis.addr=localhost:{{ include "redis.containerPort" . }} \
                --redis.password="${REDIS_PASSWORD}" \
                {{- range .Values.metrics.extraArgs }}
                {{ . }} \
                {{- end }}
              {{- else }}
              /redis_exporter \
                --redis.addr=localhost:{{ include "redis.containerPort" . }} \
                {{- range .Values.metrics.extraArgs }}
                {{ . }} \
                {{- end }}
              {{- end }}
          ports:
            - name: metrics
              containerPort: {{ include "redis.metrics.containerPort" . }}
              protocol: TCP
          resources: {{- toYaml .Values.metrics.resources | nindent 12 }}
          {{- if (include "redis.auth.enabled" .) }}
          volumeMounts:
            - name: redis-password
              mountPath: /opt/redis/password
              readOnly: true
          {{- end }}
        {{- end }}
        {{- if and .Values.sentinel.enabled .Values.master.watchdog.enabled }}
        - name: watchdog
          image: {{ include "redis.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            runAsUser: 1001
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          command:
            - /bin/sh
            - /opt/redis/scripts/watchdog-master.sh
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          volumeMounts:
            - name: scripts
              mountPath: /opt/redis/scripts
              readOnly: true
            {{- if (include "redis.auth.enabled" .) }}
            - name: redis-password
              mountPath: /opt/redis/password
              readOnly: true
            {{- end }}
        {{- end }}
        {{- if .Values.master.sidecars }}
        {{- toYaml .Values.master.sidecars | nindent 8 }}
        {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "redis.configmapName" . }}
            defaultMode: 0644
        - name: scripts
          configMap:
            name: {{ include "redis.scripts.configmapName" . }}
            defaultMode: 0755
        - name: tmp
          emptyDir: {}
        {{- if (include "redis.auth.enabled" .) }}
        - name: redis-password
          secret:
            secretName: {{ include "redis.secretName" . }}
            items:
              - key: {{ include "redis.secretPasswordKey" . }}
                path: redis-password
              {{- if and .Values.sentinel.enabled .Values.auth.sentinel }}
              - key: {{ include "redis.secretSentinelPasswordKey" . }}
                path: redis-sentinel-password
              {{- end }}
            defaultMode: 0400
        {{- end }}
        {{- if .Values.master.extraVolumes }}
        {{- toYaml .Values.master.extraVolumes | nindent 8 }}
        {{- end }}
  {{- if .Values.master.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
        {{- if .Values.master.persistence.annotations }}
        annotations: {{- toYaml .Values.master.persistence.annotations | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
          {{- range .Values.master.persistence.accessModes }}
          - {{ . | quote }}
          {{- end }}
        resources:
          requests:
            storage: {{ .Values.master.persistence.size | quote }}
        {{- if or .Values.master.persistence.storageClass (include "redis.storageClass" .) }}
        storageClassName: {{ default (include "redis.storageClass" .) .Values.master.persistence.storageClass | quote }}
        {{- end }}
        {{- if .Values.master.persistence.selector }}
        selector: {{- toYaml .Values.master.persistence.selector | nindent 10 }}
        {{- end }}
        {{- if .Values.master.persistence.dataSource }}
        dataSource: {{- toYaml .Values.master.persistence.dataSource | nindent 10 }}
        {{- end }}
  {{- else }}
        - name: data
          emptyDir: {}
  {{- end }}
{{- end }}
